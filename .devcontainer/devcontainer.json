{
  "name": "Codespaces with gcloud & Dify",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "extensions": [
    "ms-vscode.go",
    "googlecloudtools.cloudcode"
  ],
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "features": {
    "docker-in-docker": "latest"
  },
  "postCreateCommand": "gcloud init --skip-diagnostics && sudo apt-get update && sudo apt-get install -y docker-compose"
}
